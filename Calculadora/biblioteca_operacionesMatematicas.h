/** \brief Suma dos números flotantes y retorna el resultado.
 *
 * \param float Número flotante ingresado anteriormente por el usuario.
 * \param float Número flotante ingresado anteriormente por el usuario.
 * \return float Resultado de la suma de ambos números ingresados.
 *
 */
float sumarDosFlotantes (float, float);

/** \brief Resta dos números flotantes y retorna el resultado.
 *
 * \param float Número flotante ingresado anteriormente por el usuario.
 * \param float Número flotante ingresado anteriormente por el usuario.
 * \return float Resultado de la resta de ambos números ingresados.
 *
 */

float restarDosFlotantes (float, float);

/** \brief Multiplica dos números flotantes y retorna el resultado.
 *
 * \param float Número flotante ingresado anteriormente por el usuario.
 * \param float Número flotante ingresado anteriormente por el usuario.
 * \return float Resultado de la multiplicación de ambos números ingresados.
 *
 */

float multiplicarDosFlotantes (float, float);

/** \brief Divide dos números flotantes y retorna el resultado.
 *
 * \param float Número flotante ingresado anteriormente por el usuario.
 * \param float Número flotante ingresado anteriormente por el usuario.
 * \return float Resultado de la división de ambos números ingresados.
 *
 */

float dividirDosFlotantes (float, float);

/** \brief Calcula el factorial de un número y retorna el resultado.
 *
 * \param float Número flotante ingresado anteriormente por el usuario.
 * \return long long int El factorial del número ingresado
 *
 */

long long int calcularFactorial (float);
